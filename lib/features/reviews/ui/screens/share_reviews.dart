import 'package:flutter/material.dart';

class ShareReviews extends StatelessWidget {
  const ShareReviews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Share Review'),
      ),
      body: Column(
        children: [

        ],
      ),
    );
  }
}
